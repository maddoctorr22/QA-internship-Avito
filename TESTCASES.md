## ID: **API_TC_001** 🔥
## Title
**Создание объявления через `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, корректно ли API обрабатывает запрос на создание объявления и возвращает код 200**
## Reproduction Steps:  
1. Отправить `POST`-запрос на `/api/1/item` с телом запроса:
   ```json
   {
     "sellerId": 228322,
     "name": "Google Pixel 7 Pro",
     "price": 3200,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
2. Убедиться, что статус-код `200 OK`. 
3. Убедиться, что в теле ответа содержится поле "status": "Сохранили объявление - {id}"
4. Извлечь `id` из ответа
5. Отправить GET-запрос на /api/1/item/{id}, подставив полученный id.
6. Проверить, что данные в ответе совпадают с данными, отправленными в `POST`-запросе

## **✅ Expected Result:**  
- Статус-код ответа `200 OK`
- В теле ответа содержится `"status": "Сохранили объявление - id"` и валидный `id` созданного объявления
- Поле `sellerId` в ответе `GET` совпадает с отправленным `POST`.
- Поле `name` в ответе `GET` совпадает с отправленным `POST`.
- Поле `price` в ответе `GET` совпадает с отправленным `POST`.
- Поле `statistics` (включая `contacts`, `likes`, `viewCount`) в ответе `GET` совпадает с отправленным `POST`
  
## **✅ Actual Result:**  
- Поле `name` в ответе `GET` отличается от отправленного в `POST`.
- Ожидаемое: "Google Pixel 7 Pro".
- Фактическое: "dsds".
- Баг задокументирован в `BUGS.md` (BUG-001).

## ID: **API_TC_002** 🔥
## Title
**Проверка обработки верхней границы sellerId при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что запрос отрабатывает корректно с верхней границей ID и возвращает код 200**
## Reproduction Steps:
1. Отправить `POST`-запрос на `/api/1/item` с телом запроса:
   ```json
   {
     "sellerId": 999999,
     "name": "Google Pixel 7 Pro",
     "price": 3200,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
2. Убедиться, что статус-код `200 OK`. 
3. Убедиться, что в теле ответа содержится поле "status": "Сохранили объявление - {id}"
4. Извлечь `id` из ответа
5. Отправить GET-запрос на /api/1/item/{id}, подставив полученный id.
6. Проверить, что данные в ответе совпадают с данными, отправленными в `POST`-запросе

## **✅ Expected Result:**  
- Статус-код ответа `200 OK`
- В теле ответа содержится `"status": "Сохранили объявление - id"` и валидный `id` созданного объявления
- Поле `sellerId` в ответе `GET` совпадает с отправленным `POST`.
- Поле `name` в ответе `GET` совпадает с отправленным `POST`.
- Поле `price` в ответе `GET` совпадает с отправленным `POST`.
- Поле `statistics` (включая `contacts`, `likes`, `viewCount`) в ответе `GET` совпадает с отправленным `POST`.

## **✅ Actual Result:**  
- Поле `name` в ответе `GET` отличается от отправленного в `POST`.
- Ожидаемое: "Google Pixel 7 Pro".
- Фактическое: "dsds".
- Баг задокументирован в `BUGS.md` (BUG-001).

## ID: **API_TC_003** 🔥
## Title
**Проверка обработки нижней границы sellerId при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что запрос отрабатывает корректно с нижней границей ID**
## Reproduction Steps:
1. Отправить `POST`-запрос на `/api/1/item` с телом запроса:
   ```json
   {
     "sellerId": 111111,
     "name": "Google Pixel 7 Pro",
     "price": 3200,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
2. Убедиться, что статус-код `200 OK`. 
3. Убедиться, что в теле ответа содержится поле "status": "Сохранили объявление - {id}"
4. Извлечь `id` из ответа
5. Отправить GET-запрос на /api/1/item/{id}, подставив полученный id.
6. Проверить, что данные в ответе совпадают с данными, отправленными в `POST`-запросе

## **✅ Expected Result:**  
- Статус-код ответа `200 OK`
- В теле ответа содержится `"status": "Сохранили объявление - id"` и валидный `id` созданного объявления
- Поле `sellerId` в ответе `GET` совпадает с отправленным `POST`.
- Поле `name` в ответе `GET` совпадает с отправленным `POST`.
- Поле `price` в ответе `GET` совпадает с отправленным `POST`.
- Поле `statistics` (включая `contacts`, `likes`, `viewCount`) в ответе `GET` совпадает с отправленным `POST`.

## **✅ Actual Result:**  
- Поле `name` в ответе `GET` отличается от отправленного в `POST`.
- Ожидаемое: "Google Pixel 7 Pro".
- Фактическое: "dsds".
- Баг задокументирован в `BUGS.md` (BUG-001).

## ID: **API_TC_004** 🔥
## Title
**Проверка обработки обязательных полей на пустое значение при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что при отсутствии обязательного поля (sellerId, name, price, statistics), API возвращает статус-код `400 Bad Request`**
## Reproduction Steps:

1. Отправить `POST`-запрос на `/api/1/item` с пустым `sellerId`:
   ```json
   {
     "sellerId": null,
     "name": "Google Pixel 7 Pro",
     "price": 3200,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
2. Убедиться, что статус-код `400 Bad Request`.
   
3. Отправить `POST`-запрос на `/api/1/item` с пустым `name`:
   ```json
   {
     "sellerId": 228322,
     "name": "",
     "price": 3200,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
4. Убедиться, что статус-код `400 Bad Request`.
   
5. Отправить `POST`-запрос на `/api/1/item` с пустым `price`:
   ```json
   {
     "sellerId": 228322,
     "name": "Google Pixel 7 Pro",
     "price": "",
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
6. Убедиться, что статус-код `400 Bad Request`.
 
7. Отправить `POST`-запрос на `/api/1/item` с пустым `statistics`:
   ```json
   {
     "sellerId": 228322,
     "name": "Google Pixel 7 Pro",
     "price": 3200,
     "statistics": {}
   }
   ```
8. Убедиться, что **статус-код `400 Bad Request`**, отображается для каждого запроса
   
## **✅ Expected Result:**  
- Статус-код ответа `400 Bad Request` для каждого запроса
- В теле ответа содержится ошибка
  
## **✅ Actual Result:**  
   #Заполнить после выполнения

## ID: **API_TC_005** 🔥
## Title
**Проверка обработки некорректных значений в поле `sellerId` при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает некорректные значения `sellerId`, возвращая статус-код `400 Bad Request`.**
## **🔁 Reproduction Steps:**  

1. Отправить `POST`-запрос на `/api/1/item`
    ```json
   {
     "sellerId": -1,
     "name": "Google Pixel 7 Pro",
     "price": 3200,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
     Ожидать код `400 Bad Request`
2. Повторить шаг 1 с некорректными данными для `sellerId` с данными ниже:
   1.	"abc123" (строка вместо числа)	`400 Bad Request`
	2. 9999999999900 (слишком длинное число)	`400 Bad Request`
3. Убедиться, что **статус-код `400 Bad Request`**, отображается для каждого запроса
4. Проверить, что **в теле ответа содержится сообщение об ошибке**.  

---

## **✅ Expected Result:**  
- Статус-код `400 Bad Request`.  
- В теле ответа содержится ошибка

---

## **🔎 Actual Result:**  
- Статус-код `400 Bad Request`.
- Запрос некорректно отрабатывает данные
- `sellerId: -255`, проходит код `200 OK`
- `sellerId: 999999999999999`, проходит код `200 OK`
- Отсутствие поля `sellerId`, проходит код `200 OK`
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_006** 🔥
## Title
**Проверка обработки некорректных значений в поле `name` при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает некорректные значения `name`, возвращая статус-код `400 Bad Request`.**
## **🔁 Reproduction Steps:**  

1. Отправить `POST`-запрос на `/api/1/item`
    ```json
   {
     "sellerId": 228322,
     "name": "A",
     "price": 3200,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
     Ожидать код `400 Bad Request`
2. Повторить шаг 1 с некорректными данными для `name` с данными ниже:
   1.	"1234567890" (только цифры)	`400 Bad Request`
	2. "!@#$%^&*()_+" (только спецсимволы)	`400 Bad Request`
	3. "         " (только пробелы)	`400 Bad Request`
   4. A*1000 (длиннее максимального) `400 Bad Request`
   5. Удалить поле `name` `400 Bad Request`
4. Убедиться, что **статус-код `400 Bad Request`**, отображается для каждого запроса
5. Проверить, что **в теле ответа содержится сообщение об ошибке**.  

---

## **✅ Expected Result:**  
- Статус-код `400 Bad Request`
- В теле ответа содержится ошибка
---

## **🔎 Actual Result:**  
- Статус-код `200 OK`.
- `name: "1234567890"`, проходит код `200 OK`
- `name: "!@#$%^&*()_+"`, проходит код `200 OK`
- `name: "         "`, проходит код`200 OK`
- `name: "A*1000"`(1000 символов), проходит код `200 OK`
- Отсутствие поля `name`, проходит код `200 OK`
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_007** 🔥
## Title
**Проверка обработки некорректных значений в поле `price` при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает некорректные значения `price`, возвращая статус-код `400 Bad Request`.**
## **🔁 Reproduction Steps:**  

1. Отправить `POST`-запрос на `/api/1/item`
    ```json
   {
     "sellerId": 228322,
     "name": "Google Pixel 7 Pro",
     "price": -1,
     "statistics": {
       "contacts": 100,
       "likes": 228,
       "viewCount": 322
     }
   }
   ```
    Ожидать код `400 Bad Request`
2. Повторить шаг 1 с некорректными данными для `price` с данными ниже:
   1.	`price = "1000"`, проходит код `400 Bad Request`
   2. `price = null` (Значение null), код - 	`400 Bad Request`
	3. `price = 0` (нулевое значение), код - 	`400 Bad Request`
   4. `price = 1000000000000000000` (чрезмерно большое число), код -  `400 Bad Request`
   5. Удалить поле `price`, код - `400 Bad Request`
4. Убедиться, что **статус-код `400 Bad Request`**, отображается для каждого запроса
5. Проверить, что **в теле ответа содержится сообщение об ошибке**.  

---

## **✅ Expected Result:**  
- Статус-код `400 Bad Request`
- В теле ответа содержится ошибка
---

## **🔎 Actual Result:**  
- Статус-код `200 OK`, для запросов:
- `price: "-1"` проходит, код `200 OK` 
- `price: null` проходит, код `200 OK`
- Отсутствие поля `price` – код `200 OK`
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_008** 🔥
## Title
**Проверка обработки некорректных значений в поле `statistics` при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает некорректные значения `statistics`, возвращая статус-код `400 Bad Request`.**
## **🔁 Reproduction Steps:**  

1. Отправить `POST`-запрос на `/api/1/item`
    ```json
   {
     "sellerId": 228322,
     "name": "Google Pixel 7 Pro",
     "price": 3200,
     "statistics": null
   }
   ```
    Ожидать код `400 Bad Request`
2. Повторить шаг 1 с некорректными данными для `statistics` с данными ниже:
   1. `statistics: {"likes": -10, "contacts": -5, "viewCount": -20}`, проходит код `400 Bad Request`
   2. `statistics: {"likes": "string", "contacts": "string", "viewCount": "string"}`, проходит код `400 Bad Request`
   3. Удалить поле `statistics`, проходит код`400 Bad Request`
4. Убедиться, что **статус-код `400 Bad Request`**, для каждого запроса
5. Проверить, что **в теле ответа содержится сообщение об ошибке**.  

---

## **✅ Expected Result:**  
- Статус-код `400 Bad Request`, для всех запросов
- В теле ответа содержится ошибка
---

## **🔎 Actual Result:**  
- Статус-код `200 OK`, для запросов:
- `statistics: {}` проходит, код `200 OK`
- `statistics: null` проходит, код `200 OK`
- `statistics: {"likes": -10, "contacts": -5, "viewCount": -20}` проходит, код `200 OK`
- Отсутствие поля `statistics` – код `200 OK`
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_009** 🔥
## Title
**Проверка обработки отправки запроса без тела при `POST /api/1/item`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает запрос, без тела, возвращая статус-код `400 Bad Request`.**
## **🔁 Reproduction Steps:**  

1. Отправить `POST`-запрос на `/api/1/item`
    ```json
   {}
   ```
    Ожидать код `400 Bad Request`
2. Убедиться, что **статус-код `400 Bad Request`**, отображается
3. Проверить, что **в теле ответа содержится сообщение об ошибке**.  

---

## **✅ Expected Result:**  
- Статус-код `400 Bad Request`
- В теле ответа содержится корректное сообщение об ошибке, указывающее на неверный формат.
---

## **🔎 Actual Result:**  
- Статус-код `200 OK`
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_010** 🔥
## Title
**Проверка получения объявления по его идентификатору `GET /api/1/item/:id`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает запрос, и возвращает корректную информацию объявления, возвращая статус-код `200 OK`.**

---

## **🔁 Reproduction Steps:**  
1. Отправить `GET`-запрос на `/api/1/item/:id`
2. Убедиться, что **статус-код `200 OK`**
3. Убедиться, что **в теле ответа содержится `Body` объявления**
4. Убедиться, что все поля содержат корректные данные:
   - `createdAt` (в формате `ISO 8601`)
   - `id` (string, совпадает с `POST`)
   - `name` (string, совпадает с `POST`)
   - `price` (int, совпадает с `POST`)
   - `sellerId` (int, совпадает с `POST`)
   - `statistics` содержит:
     - `contacts` (int, совпадает с `POST`)
     - `likes` (int, совпадает с `POST`)
     - `viewCount` (int, совпадает с `POST`)

---

## **✅ Expected Result:**  
- **Статус-код:** `200 OK`
- **В теле ответа содержится:**
   ```json
  {
        "createdAt": "2025-02-15T17:03:05.777+03:00",
        "id": "89463afe-5bfa-47d1-bc11-f6dd9e2008ce",
        "name": "Google Pixel 7 Pro",
        "price": 3200,
        "sellerId": 228322,
        "statistics": {
            "contacts": 100,
            "likes": 228,
            "viewCount": 322
        }
  }
   
## **🔎 Actual Result:**  
- Статус-код `200 OK`
- `name` не совпадает с `POST`
- `createdAt` имеет некорректный формат
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_011** 🔥
## Title
**Проверка обработки запроса с некорректным идентификатором `GET /api/1/item/:id`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает запрос, и возвращает сообщение об ошибке, возвращая статус-код `400 Bad Request`.**

---

## **🔁 Reproduction Steps:**  
1. Отправить `GET`-запрос на `/api/1/item/:id`, со следующими данными:
- `:id` = 1234567890, код `400 Bad Request`
- `:id` = "1234567890", код `400 Bad Request`
- `:id` = null, код `400 Bad Request`
- `:id` = "", код `400 Bad Request`
- `:id` = "      ", код `400 Bad Request`
  
2. Убедиться, что **статус-код `400 Bad Request`**, для всех запросов
3. Убедиться, что **тело ответа содержит сообщение об ошибке**.
---

## **✅ Expected Result:**  
- **Статус-код:** `400 Bad Request`
- **В теле ответа содержится:**
   ```json
  {
    "result": {
        "message": "передан некорректный идентификатор объявления",
        "messages": {}
    },
    "status": "400"
}
   
## **🔎 Actual Result:**  
- Статус-код `400 Bad Request`
- Тело ответа совпадает с ожидаемым

## ID: **API_TC_012** 🔥
## Title
**Проверка получения статистики объявления по его идентификатору `GET /api/1/statistic/{id}`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает запрос, и возвращает корректную информацию объявления, возвращая статус-код `200 OK`.**

---

## **🔁 Reproduction Steps:**  
1. Отправить `GET`-запрос на `/api/1/statistic/{id}`, с валидными данными
2. Убедиться, что запрос имеет **статус-код `200 OK`**
3. Убедиться, что `likes` `contacts` `viewCount` >= 0
4. Убедиться, что **тело ответа содержит статистику объявления**.
   ```json
   {
        "contacts": 100,
        "likes": 456,
        "viewCount": 778
    }
   ```
---

## **✅ Expected Result:**  
- **Статус-код:** `200 OK`
- Все значения `contacts`, `likes`, `viewCount` ≥ 0.
- **В теле ответа содержится:**
  ```json
    {
        "contacts": 1000,
        "likes": 228,
        "viewCount": 322
    }
   ```
   
## **🔎 Actual Result:**  
- Статус-код `200 OK`
- Все значения `contacts`, `likes`, `viewCount` ≥ 0.
- Фактическое тело ответа:
  ```json
  {
     "contacts": 100,
     "likes": 456,
     "viewCount": 778
  }
  ```
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_013** 🔥
## Title
**Проверка обработки запроса получения статистики с некорректным идентификатором `GET /api/1/statistic/{id}`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает запрос, и возвращает сообщение об ошибке, возвращая статус-код `400 Bad Request`**

---

## **🔁 Reproduction Steps:**  
1. Отправить `GET`-запрос на `/api/1/statistic/{id}`, со следующими данными:
- `:id` = 1234567890, код `400 Bad Request`
- `:id` = "1234567890", код `400 Bad Request`
- `:id` = "1234567890qwerty", код `400 Bad Request`
- `:id` = "!@#$%^&*()_+", код `400 Bad Request`
- `:id` = null, код `400 Bad Request`
- `:id` = "", код `400 Bad Request`
- `:id` = "      ", код `400 Bad Request`
  
2. Убедиться, что **статус-код `400 Bad Request`**, для всех запросов
3. Убедиться, что **тело ответа содержит сообщение об ошибке**.
---

## **✅ Expected Result:**  
- **Статус-код:** `400 Bad Request`
- **В теле ответа содержится:**
   ```json
  {
    "result": {
        "message": "передан некорректный идентификатор объявления",
        "messages": {}
    },
    "status": "400"
}
   
## **🔎 Actual Result:**  
- Статус-код `400 Bad Request`
- Тело ответа совпадает с ожидаемым

## ID: **API_TC_014** 🔥
## Title
**Проверка получения списка объявлений продавца по идентификатору `GET /api/1/{sellerId}/item`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает запрос, и возвращает все созданные объявления продавцом, возвращая статус-код `200 OK`**

---

## **🔁 Reproduction Steps:**  
1. Отправить `GET`-запрос на `/api/1/{sellerId}/item`, с валидными данными
2. Отправить `GET`-запрос на `/api/1/{sellerId}/item`, с валидными данными, на продавца у которого еще нет объявлений
3. Убедиться, что **статус-код `200 OK`**, для всех запросов
4. Убедиться, что **тело ответа содержит все объявления продавца и пустой массив, если у продавца еще нет объявлений**.
---

## **✅ Expected Result:**  
- **Статус-код:** `200 OK`
- **В теле ответа содержится массив данных: Если у продавца есть объявления**
   ```json
  {
        "createdAt": "2025-02-15T17:03:05.777+03:00",
        "id": "89463afe-5bfa-47d1-bc11-f6dd9e2008ce",
        "name": "Google Pixel 7 Pro",
        "price": 3200,
        "sellerId": 228322,
        "statistics": {
            "contacts": 100,
            "likes": 228,
            "viewCount": 322
        }
  }
- Если нет объявлений **Пустой массив**
  ```json
  []
  ```
   
## **🔎 Actual Result:**  
- Статус-код `200 OK`
- Фактическое тело ответа:
 ```json
{
	"createdAt": "2025-02-15 11:31:26.222894 +0300 +0300",
	"id": "dsdsd",
	"name": "89463afe-5bfa-47d1-bc11-f6dd9e2008ce",
	"price": 3200,
	"sellerId": 228322,
	"statistics": {
	    "contacts": 100,
	    "likes": 228,
	    "viewCount": 322
	}
 }
  ```
- Поля `createdAt`, `id`, `name`, не соответствуют ожиданию
- Баг задокументирован в `BUGS.md`

## ID: **API_TC_015** 🔥
## Title
**Проверка получения списка объявлений продавца по некорректному идентификатору `GET /api/1/{sellerId}/item`**

## Priority
**Высокий**
## Description:
**Проверить, что API корректно обрабатывает запрос, и возвращает сообщение об ошибке, возвращая статус-код `400 Bad Request`**

---

## **🔁 Reproduction Steps:**  
1. Отправить `GET`-запрос на `/api/1/{sellerId}/item`, со следующими данными:
   - `sellerId = 99999999999999999999999999`, код `400 Bad Request`
   - `sellerId = "1234567890"`, код `400 Bad Request`
   - `sellerId = "1234567890qwerty"`, код `400 Bad Request`
   - `sellerId = "!@#$%^&*()_+"`, код `400 Bad Request`
   - `sellerId = null`, код `400 Bad Request`
   - `sellerId = ""`, код `400 Bad Request`
   - `sellerId = "      "`, код `400 Bad Request`
   - `sellerId = 111110`, код `400 Bad Request`
   - `sellerId = 1000000`, код `400 Bad Request`
  
2. Убедиться, что **статус-код `400 Bad Request`**, для всех запросов
3. Убедиться, что **тело ответа содержит сообщение об ошибке**.
---

## **✅ Expected Result:**  
- **Статус-код:** `400 Bad Request`
- **В теле ответа содержится:**
   ```json
  {
    "result": {
        "message": "передан некорректный идентификатор продавца",
        "messages": {}
    },
    "status": "400"
  }
   ```
   
## **🔎 Actual Result:**  
- Статус-код `400 Bad Request`, для запросов граничных значений +1
- `sellerId = 111110`, код `200 OK`, Меньше минимального
- `sellerId = 1000000`, код `200 OK`, больше максимального
- Баг задокументирован в `BUGS.md`

